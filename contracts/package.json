{
  "name": "prediction-market-contracts",
  "version": "1.0.0",
  "description": "Prediction Market smart contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "demo:setup": "hardhat run scripts/demo-setup.ts --network localhost",
    "demo:resolve": "hardhat run scripts/demo-resolve.ts --network localhost",
    "node": "hardhat node"
  },
  "dependencies": {
    "@chainlink/contracts": "^1.3.0",
    "@openzeppelin/contracts": "^5.3.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^22.0.0",
    "hardhat": "^2.22.18",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
